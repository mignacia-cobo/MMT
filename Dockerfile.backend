FROM node:20-alpine

# bcrypt requiere compilación nativa
RUN apk add --no-cache python3 make g++

WORKDIR /app

COPY backend/package*.json ./
RUN npm ci --only=production

COPY backend/ .

# Carpeta de uploads persistida via volume
RUN mkdir -p uploads

EXPOSE 3001
CMD ["node", "server.js"]
